@model IEnumerable<Enterprise_Web.Models.Closure_Day>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Set Closure Date</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>

<div class="table-responsive">
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.startDay)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.endDay)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.finalDay)
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.startDay)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.endDay)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.finalDay)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.csdID }) |
                    @Html.ActionLink("Details", "Details", new { id = item.csdID }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.csdID })
                </td>
            </tr>
        }

    </table>
    </div>
<div>
    @Html.ActionLink("Back to List", "Index", "Home")
</div>