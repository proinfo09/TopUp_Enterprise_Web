@model IEnumerable<Enterprise_Web.Models.User_Marketing_Coordinator_Detail>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>

<div class="table-responsive">
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.mkc_fullname)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.mkc_mail)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.mkc_gender)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.mkc_doB)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.mkc_phone)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AspNetUser.Email)
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.mkc_fullname)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.mkc_mail)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.mkc_gender)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.mkc_doB)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.mkc_phone)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.AspNetUser.Email)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.mkcID }) |
                    @Html.ActionLink("Details", "Details", new { id = item.mkcID }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.mkcID })
                </td>
            </tr>
        }

    </table>
    </div>
